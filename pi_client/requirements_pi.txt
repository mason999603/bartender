# Russell Pi Client — runtime deps.
# Install on the Pi with: pip install -r requirements_pi.txt

# Cloud comms
requests>=2.31.0
python-dotenv>=1.0.0

# Audio I/O (PortAudio binding — easier to install on Pi than pyaudio)
# On Raspberry Pi OS: sudo apt install -y libportaudio2 portaudio19-dev
sounddevice>=0.4.6
numpy>=1.24.0
soundfile>=0.12.1

# Wake word engine — fully open-source, no API keys, runs on-device
# We install it manually with --no-deps to skip tflite-runtime (which doesn't
# have Python 3.13 wheels) — we run it in ONNX mode via onnxruntime instead.
# See README "On the Pi" section for the install command.
onnxruntime>=1.16.0
tqdm>=4.0
scipy>=1.3
scikit-learn>=1.0
mutagen>=1.45

# TTS — local Piper is gone; we use cloud /api/voice/speak instead.
# Nothing to install for TTS beyond `aplay` (already in alsa-utils).
